#include <iostream>
#include <cmath>

int main()
{
    int n;

    do
    {
        std::cout << "Enter natural number n:";
        std::cin >> n;
    }
    while(n < = 0);

    double sqrt n = std::sqrt(n);

    std::cout << "sqrt(" <<n << ") = " << sqrt n << std::endl;




    return 0;
}
