#include <iostream>

int main()
{
    int x = 2,y = 3;

    if(x > 4,y > 1) //this variables true because only takes last segment(doesn't matter x but important y because last segment) and so comma operator is very dangerous and very trick :)
        std::Cout << "Anything" <<std::endl;

    if(2 > 4 && y > 1) //last segment very important and this variables false
        std::cout << "Get out my way" << std::endl;

    AND true true true
        true false false or false true false
        false false false


    if(A)
    {
        some code
        if
        else if
        else

    }
    else if (B)
    {
        if(C)
            some code

        else if(!C)
            some code
    }

}
